# Create a vector of student names
students = c("Bikram", "Koushik", "Arijit", "Aarav", "Goutam")

# Randomly select a topper
toppers = sample(students, 1)

# Display the topper
cat("🏆 The topper of the class is:", toppers, "\n")
